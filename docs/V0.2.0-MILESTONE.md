# Lattice v0.2.0 — Milestone Plan

**Decision (Dylan, 2026-06-11):** stop cutting a public release per small patch. Build **v0.2.0**
as a deliberate milestone over **~4–5 full sessions** of fixing, tweaking, auditing, and real
feature work. Accumulate everything on local `master`; **do NOT tag/push a release until the
milestone is complete** (same discipline as the held-back v0.1.5 big update).

Starting point: **v0.1.9** — fleet 5/5, one-click update path production-proven, dashboard fully
offline. The 0.1.x stream hardened the update/cascade machinery; v0.2.0 is about depth: the IDE
feature, closing residuals, hardening identity, and an audit-driven quality pass.

---

## Workstreams (candidates — "they all sound relevant and important" — Dylan)

### A. Mesh editor (IDE Milestone M2-P2) — the headline feature
The embedded VS Code (code-server) editor works on mini-ops only. Make it real across the fleet:
- Install + verify code-server on the darwin machines (studio/mbp/emu) so `editor` sessions launch
  on any Mac (D30); placement already excludes machines without it.
- Windows via **WSL2** (D30) — the harder path; the agent probes WSL + runs the Linux code-server inside.
- Retire the read-only Monaco rail; the embedded editor becomes the editor (D31).
- Decide D28 distribution: per-node install (simple, ~384MB/box) vs hub-served tarball (~95MB, "no
  per-machine install"). Spike notes already in docs/STATE.md "IDE Milestone (M2)".
- (Later/P3) Cursor-grade AI chrome beside the editor (D29).

### B. agentID collision hardening
Two machines with the same hostname collide on one id (`<host>-<os>`) → reconnect duel (the storm
family). Add a persisted per-node nonce to the id. **Risk:** changes enrollment identity on the LIVE
fleet — must preserve existing ids (no orphaned sessions / duplicate fleet rows). Migration-careful.

### C. Residual live-verifications (need Dylan present)
- **/fpreview/ HMR-on-phone** — open a Vite/Next preview on a phone, confirm hot-reload over the tunnel.
- **Telemetry $cost / context% + auto-name** — run one real Claude session; verify the transcript-derived
  telemetry + the free auto-name heuristic render live (closes the v0.1.5 residuals). Needs a billed turn.

### D. Audit-driven quality pass (session 1 — IN PROGRESS)
Four parallel read-only audits seeded this milestone: **security** (auth gating, capability URLs, SSRF in
preview/fpreview, tokens, supply-chain), **backend resilience** (reconnect-storm class, cascade,
SQLite, leaks, timeouts), **frontend/UX** (a11y, state coverage, mobile, type-safety, design drift,
error boundary), **ops/distribution** (installers, cross-OS, release pipeline, docs accuracy, agentID).
Findings get triaged below into fix-now vs backlog.

### E. Polish & doc hygiene
README/STATE/ROADMAP accuracy at v0.1.9; stale "Known minor items"; CHANGELOG `[0.2.0]` assembled at the end.

---

## Rough session plan (will adjust as the audit lands)
- **Session 1 (this one):** audit pass + scope (this doc) + fix the high-confidence audit wins. Decide D28.
- **Session 2:** mesh editor on the darwin fleet (code-server install/verify; editor launches on studio/mbp/emu).
- **Session 3:** agentID collision hardening (careful live-fleet migration) + remaining backend/security fixes.
- **Session 4:** Windows/WSL2 editor + Monaco-rail retirement (D31) + frontend/UX fixes.
- **Session 5:** residual live-verifications (with Dylan), full regression (one-click cascade), assemble
  CHANGELOG `[0.2.0]`, tag + push + release.
(Order is a guide, not a contract — fix what's most broken first.)

---

## Audit findings — triage (session 1)
Four parallel read-only audits (security, backend resilience, frontend/UX, ops/distribution).
`✅ S1` = fixed this session. `📋` = backlogged with a target session.

### ✅ Fixed in session 1 (low-risk, high-confidence)
| Sev | Area | Finding | Fix |
|-----|------|---------|-----|
| CRIT | ops | `get.sh` `set -euo pipefail` under `#!/bin/sh` aborts the advertised Linux `curl\|sh` install (dash/BusyBox reject `-o pipefail`) | → `set -eu` (`install/get.sh`) |
| CRIT | frontend | No error boundary — any render throw white-screens the whole console | new `ErrorBoundary` wrapping App + the lazy Workspace (`components/ErrorBoundary.tsx`, `main.tsx`, `App.tsx`) |
| HIGH | backend | Bare goroutines have no `recover()` — one panic crashes the controller, dropping all agents+tunnels | `superviseLoop` (restart-on-panic) for the 5 long-lived loops + `goSafe` for the transcript-derivation one-shots (`hub.go`, `approvals.go`, `hooks.go`) |
| MED | security | Per-machine enroll tokens only 64-bit | widened to 128-bit (`enrolltokens.go`); existing tokens stay valid |
| MED | backend | `audit_log` reap was a whole-table anti-join every 2 min on the single write conn | index-friendly `id <= (… LIMIT 1 OFFSET ?)` range delete (`store.go`) |
| MED | ops | ROADMAP stale ("M2 is current") — M3/v0.1.x shipped | updated `docs/ROADMAP.md` (added M3-shipped, framed v0.2.0) |

### 📋 Backlog (target session in brackets)
| Sev | Area | Finding | file | Target |
|-----|------|---------|------|--------|
| CRIT | backend | **Agent identity has no per-connection fencing + no storm detector** — reconnect-storm class is fixed only cause-by-cause | `agentws.go:369`, `registry.go` | **S2-S3 (centerpiece: agent UUID + InstanceID + duel alarm)** |
| HIGH | ops | agentID `hostname+os` collision — same-named boxes duel + cross-wire sessions/editor/terminal | `agentws.go`, `agent/tunnel.go` | S2-S3 (same fix as above: persistent agent UUID, hostname display-only) |
| HIGH | security | Passwordless hub = open fleet-wide RCE on the tailnet (`/api/agents/*/exec`, `/api/update` ungated when no password) | `auth.go`, `http.go` | S3 (fail-closed post-setup; gate exec/update/power w/ token even passwordless) |
| HIGH | security | First-run setup wizard is an unauth take-over window (first tailnet peer claims admin) | `setup.go` | S3 (require on-disk master token or loopback for `/api/setup`) |
| HIGH | backend | `updateAgents` holds the single-flight lock for the whole cascade (≤7.5min); a wedged agent serializes the rest | `update.go` | S3 (per-agent ctx timeout; cap total wall-clock) |
| HIGH | backend | pm2 hub swaps binary but doesn't self-restart, yet reports `ok:true` — silent "still on old code" | `update.go:150` | S3 (return `restartRequired` instead of green check) |
| HIGH | ops | Windows-on-ARM cold install silently pulls amd64 (no arch detect; no arm64 asset) | `get.ps1`, `install.ps1.tmpl`, `release.yml` | S4 (cross-OS) |
| HIGH | ops | Linux reboot survival needs a manual `loginctl enable-linger` the installer only prints | `get.sh`, `install.sh.tmpl` | S4 (auto enable-linger best-effort) |
| CRIT | frontend | Fleet view + TopBar have zero `@media` queries → overflow on phones (Fleet is the default view) | `design/app.css` | S4 (responsive pass) |
| HIGH | frontend | FleetMap forever-60fps canvas rAF (battery drain, ignores `prefers-reduced-motion` + tab-hidden) | `lattice/FleetMap.tsx` | S4 |
| HIGH | frontend | FleetMap + rail machine-selection is mouse-only (no keyboard/AT) | `FleetMap.tsx`, `Fleet.tsx` | S4 (a11y) |
| HIGH | frontend | CommandPalette + shared Modal have no focus trap (Tab escapes behind scrim) | `CommandPalette.tsx`, `Modal.tsx` | S4 (`useFocusTrap`) |
| MED | backend | Multi-row SQLite reaps (`purgeDeleted`, `MarkAgentSessionsExitedExcept`, `DeleteSessionRow`) lack transactions | `store.go` | S3 |
| MED | backend | `offlineAfter`(15s) vs `agentReadTimeout`(20s) 5s skew → ambiguous dead-but-registered window | `hub.go` | S2-S3 (fold into identity work) |
| MED | security | `/api/hooks/state` + `/api/approvals/{nonce}` not rate-limited | `hooks.go`, `approvals.go` | S3 |
| MED | security | `get.sh`/agent download base not HTTPS-pinned (checksum-from-same-origin) | `update.go` | S3 |
| MED | frontend | `useReleases` swallows errors — version badge can't tell "current" from "couldn't check" | `useReleases.ts` | S4 |
| MED | frontend | DockPreview iframe: no load/error/empty state on a bad port | `DockPanel.tsx` | S4 |
| MED | frontend | Workspace uses hardcoded Tailwind zinc/emerald instead of design tokens (contract drift) | `workspace/Workspace.tsx` | S4 |
| MED | ops | Windows editor needs WSL2 but nothing installs/documents it | `install.ps1.tmpl`, docs | S4 |
| MED | ops | No auto-installed hub-host watchdog for a single-box self-host (only Dylan's SSH-fleet one) | `scripts/`, `install/` | S4 |
| LOW | — | Remaining detached goroutines (tunnelproxy pumps, update self-restart, notify) unwrapped; readLoop per-frame recover; release.yml gate ctx; CSRF token; register OS/hostname validation; sessions table unbounded | various | S3-S4 sweep |

---

## Done so far
- **2026-06-11 (session 1):** milestone framed; per-patch cadence stopped. Ran a 4-agent parallel
  audit → triaged above. **Shipped 6 fixes** (1 CRIT install, 1 CRIT frontend boundary, 1 HIGH
  resilience, 3 MED): get.sh dash fix, ErrorBoundary, hub goroutine supervise/recover, 128-bit enroll
  tokens, index-friendly audit_log reap, ROADMAP refresh. All on local master (held, not pushed).
  Next session: the agentID identity / storm-fencing centerpiece (the CRIT through-line).
